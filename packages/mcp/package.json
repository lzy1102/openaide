{
  "name": "@openaide/mcp",
  "version": "0.3.0",
  "description": "MCP bridge — expose Model Context Protocol servers as OpenAIDE tools (Claude-compatible config)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "test": "node ../../scripts/test.mjs mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@openaide/core": "*",
    "@openaide/plugins": "*"
  },
  "devDependencies": {
    "typescript": "~5.6.0"
  },
  "license": "MIT"
}
